pipeline {
    agent {
        label 'headche-label'
    }
    stages{
        stage('jdk-17') {
            steps {
                echo "Hello.welcome to maven section"
                sh "java --version"
            }
        }
    }
}
