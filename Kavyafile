pipeline {
    //agent block
    agent {
        label "headche-label"
    }
    //tools block at the pipeline level
    tools {
       java 'jdk-17' 
    }
    //stage block
    stages {
        stage('Build') {
            steps {
                echo "this is about the java version"
            }
        }
    }
}
